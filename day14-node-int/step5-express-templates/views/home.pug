doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
        link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet")
    body
        div(class="container")
            h1 Hello From PUG Template Engine 
            h2= company 
            form(action="#", method="post")
                label(for="nhero") Add Avenger :
                input(required id="nhero" name="valtechhero" )
                button(type="submit", class="btn btn-primary") Add Hero 
            hr
            ol(class="list-group")
                each hero in avengers
                    li(class="list-group-item")= hero